#!/usr/bin/env bash

# {{name}}
# This file contains common commands for {{name}} project. It is
# designed to be used with Nepthar's shell tools
# (https://github.com/nepthar/shtools), but my simply be sourced in
# bash instead. See the repo for more info.

export ws_name="{{name}}" # Required

# Defined "command" functions as {{name}}.cmdname for easy tab
# completion and shtools integration.

# ex: {{name}}.runTests() { ... run the tests ... }
